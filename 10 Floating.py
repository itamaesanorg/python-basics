# Lets do some type functions but now with decimals too.

# We will get at this point 3 types of characters: strings, integers, and floats.

name = "Mike"

age = 31

actual_age = 31.96 # This is a floating point number.

print(name)
print(age)

print(type(name))

print(type(age))

print(type(actual_age))

